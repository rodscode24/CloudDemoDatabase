// Find these in your Supabase project:
// Project Settings -> API -> Project URL / anon public key
//
// NOTE: The anon key is meant to be public — it's safe to commit and
// deploy. Access control is enforced by the Row Level Security (RLS)
// policies you set up in schema.sql, not by hiding this key.

const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY';
